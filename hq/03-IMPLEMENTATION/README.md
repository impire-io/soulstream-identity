# 03-IMPLEMENTATION — the live plan

[`ROADMAP.md`](ROADMAP.md) decides what gets built, in what order, behind
which gate. Milestones point at the design (and D-numbers) they realize.
Landing work means: gate green (`make check`), roadmap updated, journey
episode written, design docs propagated — in the same merge.
